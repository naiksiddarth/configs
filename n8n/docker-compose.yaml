services:
  n8n:
    image: docker.n8n.io/n8nio/n8n:latest
    restart: always
    ports:
      - 5678:5678
    env_file:
      - .env
    environment:
      - DB_TYPE=sqlite
    volumes:
      - ./n8n_data:/home/node/.n8n

networks:
  default:
    name: n8n_network
